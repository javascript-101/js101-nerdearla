
  const myFunction = function(name) {
    console.log(name)
  }

  myFunction('lei')

  // lei
  const array = [1, 2, 3, 4,5]

  for (let pepe = 0; pepe < array.length; pepe++) {
    const element = array[pepe];
    console.log(element)
  }

  while (condition) {

  }

  array.forEach(funtion(element) {
    console.log(element)
  })
